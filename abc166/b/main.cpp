#include <bits/stdc++.h>
using namespace std;

int main() {
    int n, k;
    cin >> n >> k;
    int d[k], a[k][k];
    for (int i = 0; i < k; i++)
    {
        cin >> d[i];
        for (int j = 0; j < k; j++)
        {
            cin >> a[i][j];
        }
        
    }
    
    return 0;
}
