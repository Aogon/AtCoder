#include <bits/stdc++.h>
using namespace std;

int main() {
  int N;
  cin >> N;
  int A[N], B[N];
  int i;
  int sum = 0;
  for(i=0; i<N; i++){
    cin >> A[i] >> B[i];
    int j = 0;
    for(j=A[i]; j<=B[i]; j++){
      sum = sum + j;
    }
  }
  cout << sum << endl;
}
